# 未确认项目：Orca 与 hiring-agent

## Orca

原始链接：

```text
https://github.com/topoteretes/orca
```

当前搜索结果：

- 精确 GitHub API 查询返回 404。
- 按 `Orca`、`topoteretes`、`multiple coding agents`、`isolated` 等关键词未找到匹配仓库。

判断：

- 暂不安装。
- 需要用户提供新链接或更多线索后再评估。

## hiring-agent

原始链接：

```text
https://github.com/topoteretes/hiring-agent
```

当前搜索结果：

- 精确 GitHub API 查询返回 404。
- 按 `hiring-agent`、`topoteretes`、`resume analysis` 等关键词未找到匹配仓库。

判断：

- 暂不安装。
- 招聘/简历分析本身属于高风险辅助判断场景，即便找到工具也应保留人工审核。
