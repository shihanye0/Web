# G1 / Quest 常见脚本指令

> 所有命令均为单行，可直接复制。`G1 控制主机` 是 `user@user`；`相机主机` 是 `unitree@ubuntu`。实机启动前先确认 G1 已吊装、现场清空、操作者与终端操作人为同一安全流程。


一键启动图像服务脚本：
cd /home/user/github-product/g1-dex3-care-vla && ./scripts/g1_camera_preview_run.sh --preview

自备相机：
ssh -o BatchMode=yes -o ConnectTimeout=5 unitree@192.168.123.164 'exec gst-launch-1.0 -q v4l2src device=/dev/video4 ! "image/jpeg,width=640,height=480,framerate=30/1" ! fdsink fd=1' | gst-launch-1.0 -q fdsrc fd=0 ! jpegparse ! jpegdec ! videoconvert ! autovideosink sync=false


## 0. 先分清两台主机与环境

- `.164`（`unitree@192.168.123.164`）是 G1 PC2/相机服务主机：只启动并保持 `teleimager-server`。
- `.99`（`user@user`，控制主机）运行启动检查、官方 XR 遥操、EpisodeV2 转换和 release；**不需要 SSH 进入 `.164` 后再运行遥操**。
- 相机服务必须先启动并持续运行；遥操运行时使用 `.164:55555` 的 ZMQ 图像流。Quest 不直接访问 `.164:60001`。

从控制主机进入 `.164`（已经在 `.164` 终端时跳过）：

```bash
ssh unitree@192.168.123.164
```

在 `.164` 激活相机环境（提示符已经是 `(teleimager)` 时跳过）：

```bash
conda activate teleimager
```

控制主机的官方 source runner 与 V3 通用 runner 已固定使用 `/home/user/miniconda3/envs/tv/bin/python`，因此执行下文的 runner 命令前**不必**手工 `conda activate tv`；不要用系统 `python` 替换它。runner 会在初始化 controller 前锁定 XR、Unitree SDK 的 commit 和干净状态，并强制 `PYTHONNOUSERSITE=1`，避免旧的 `~/.local` editable 影响采集。

## 1. 相机服务（`.164` / 相机主机执行）

必须先启动；即使 Quest 选择不显示机器人画面，官方遥操录制仍需要相机服务。

```bash
XR_TELEOP_CERT=/home/unitree/.config/xr_teleoperate/192.168.123.164-cert.pem XR_TELEOP_KEY=/home/unitree/.config/xr_teleoperate/192.168.123.164-key.pem teleimager-server --rs --no-affinity
```

正常标志：日志出现 `head_camera is ready` 与 `Running... Press Ctrl+C to exit.`。`uvcvideo is in use` 的 reload 警告可忽略，只要相机随后 ready 且服务持续运行。


实时图像预览：

```bash
cd /home/user/github-product/xr_teleoperate && PYTHONNOUSERSITE=1 /home/user/miniconda3/envs/tv/bin/python -m teleimager.image_client --host 192.168.123.164
```
检查端口（`.164`；仅检查，不控制机器人）：

```bash
ss -ltnp 'sport = :55555 or sport = :60000 or sport = :60001'
```

## 2. 官方 source 验证采集（`.99` / G1 控制主机执行）

用途：验证官方 XR、Quest、G1、Dex3、相机与原生 `data.json`/JPEG 链路；输出是 immutable source，**不能直接用于 VLA 训练**。每次固定 180 秒后自动保存并退出。

```bash
cd /home/user/github-product/g1-dex3-care-vla && bash scripts/g1_official_xr_touch_cup_run.sh --confirm-official-xr-touch-cup-source
```

控制 profile：不传 `--motion`，与队友的官方命令一致。启动时官方会进入 Debug Mode，并从 `rt/lowcmd` 控制手臂；这不是 `ego` 显示模式造成的。Quest 操作：`r` 对齐并开始跟随；`s` 开始采集；正常等待 180 秒会自动保存并退出，`q` 仅用于提前停止。该入口用于链路验证，不作为正式训练数据入口。

## 3. 正式 V3 通用任务训练候选采集（`.99` / G1 控制主机执行）

用途：仍由官方 `teleop_hand_and_arm.py` 执行实际遥操；额外记录 raw、mapped、实际 sent command、writer result、after-observation、RGB 引用、人工结果标签等事实。只有后续验证并导出为 `accepted EpisodeV2` 的段落才允许进入 VLA 训练。

```bash
V1:
cd /home/user/github-product/g1-dex3-care-vla && bash scripts/g1_official_xr_session_run.sh --confirm-official-xr-session --task-version medicine-pour-and-cup-relocation-v1 --task-goal '抓取预置药盒，移至预置橙色药杯并受控倾倒；放回或保持药盒，再将药杯与蓝色水杯移至各自指定位置。' --task-steps 'grasp-preopened-medicine-box,move-to-orange-cup,controlled-pour,return-or-hold-box,relocate-orange-cup,relocate-blue-cup'


V2:
cd /home/user/github-product/g1-dex3-care-vla && bash scripts/g1_official_xr_session_run.sh --confirm-official-xr-session --task-version medicine-pour-and-cup-relocation-big-box-v1 --task-goal '抓取预置大药盒，移至预置橙色药杯并受控倾倒；放回或保持药盒，再将药杯与蓝色水杯移至各自指定位置。' --task-steps 'grasp-preopened-big-medicine-box,move-to-orange-cup,controlled-pour,return-or-hold-box,relocate-orange-cup,relocate-blue-cup'


V3:
cd /home/user/github-product/g1-dex3-care-vla && bash scripts/g1_official_xr_session_run.sh --confirm-official-xr-session --task-version medicine-two-big-box-pour-and-cup-relocation-v1 --task-goal '依次抓取预置的两个大药盒，分别移至预置橙色药杯并受控倾倒；放回或保持药盒后，再将药杯与蓝色水杯移至各自指定位置。' --task-steps 'grasp-first-preopened-big-medicine-box,move-first-box-to-orange-cup,controlled-pour-first-box,return-or-hold-first-box,grasp-second-preopened-big-medicine-box,move-second-box-to-orange-cup,controlled-pour-second-box,return-or-hold-second-box,relocate-orange-cup,relocate-blue-cup'

```

cd /home/user/github-product/g1-dex3-care-vla && ./scripts/g1_competition_audio_vla_visual_run.sh

 执行候选新脚本：

  cd /home/user/github-product/g1-dex3-care-vla && bash scripts/g1_official_xr_continuity_qualification_run.sh --confirm-official-xr-continuity-qualification --task-version medicine-pour-and-cup-relocation-big-box-v1 --task-goal '抓取预置大药盒，移至预置橙色药杯并受控倾倒；放回或保持药盒，再将药杯与蓝色水杯移至各自指定位置。' --task-steps 'grasp-preopened-big-medicine-box,move-to-orange-cup,controlled-pour,return-or-hold-box,relocate-orange-cup,relocate-blue-cup'

新杯子：
  cd /home/user/github-product/g1-dex3-care-vla && bash scripts/g1_official_xr_continuity_qualification_run.sh --confirm-official-xr-continuity-qualification --task-version medicine-pour-and-cup-relocation-new-box-v1 --task-goal '抓取预置新药盒，移至预置橙色药杯并受控倾倒；放回或保持药盒，再将药杯与蓝色水杯移至各自指定位置。' --task-steps 'grasp-preopened-mew-medicine-box,move-to-orange-cup,controlled-pour,return-or-hold-box,relocate-orange-cup,relocate-blue-cup'


只拿蓝色水杯：
 cd /home/user/github-product/g1-dex3-care-vla && bash scripts/g1_official_xr_continuity_qualification_run.sh --confirm-official-xr-continuity-qualification --task-version handover-blue-water-cup-v1 --task-goal '拿起蓝色水杯并递水。' --task-steps 'grasp-blue-water-cup,hand-over-blue-water-cup'


抓药瓶倒药操作：
cd /home/user/github-product/g1-dex3-care-vla && bash scripts/g1_official_xr_continuity_qualification_run.sh --confirm-official-xr-continuity-qualification --task-version grasp-pink-medicine-box-and-pour-into-medicine-cup-v1 --task-goal '抓取粉色药盒并把药倒入药杯中' --task-steps 'grasp-pink-medicine-box,pour-medicine-into-medicine-cup'


“抓取橙色药杯并递给老人”：
  cd /home/user/github-product/g1-dex3-care-vla && bash scripts/g1_official_xr_continuity_qualification_run.sh --confirm-official-xr-continuity-qualification --task-version grasp-orange-medicine-cup-and-handover-to-elderly-v1 --task-goal '抓取橙色药杯并递给老人' --task-steps 'grasp-orange-medicine-cup,handover-medicine-cup-to-elderly'


  交互仍是原来的 r/s/a/f/o/q。退出后会自动生成：

  data/qualifications/official-xr-continuity/<session-id>/
  ├── native/
  ├── trace/
  ├── xr-input.jsonl
  └── analysis.json

  日志通过条件是：

  - continuity_qualification_verdict=pass
  - hold_violation_count=0
  - recovery_violation_count=0
  - 实际捕获到一次输入不新鲜 → HOLD → RECOVERING
  - 现场没有异常响声、跳变、意外动作或明显操作中断

  若没有触发过输入停顿，结果会是 inconclusive，不能据此替换旧脚本。采集结束后把终端最后输出和现场感受发来，我直接分析该 session。


控制 profile：`g1_official_xr_session_run.sh` 已内置 `--motion`，走官方运动控制通道（`kTopicLowCommand_Motion`），不再是 Debug Mode / `rt/lowcmd`。启动前 G1 必须先切到 AI/Regular mode（退出 Debug mode），否则 motion 控制无法生效。两种方式任选其一：

遥控器（推荐）：按 `R1+X` 切到 AI/Regular mode。

ser/miniconda3/envs/tv/bin/python -m teleimager.image_client --host 192.168.123.164

## 4. P425 隔离倒药实机运行（`.99` / G1 控制主机）

> 更新：2026-08-26。此入口是队友 A185 runner 的**隔离诊断副本**，固定 P425 120k、seed `292285`，经本机 loopback tunnel 访问远端隔离服务 `:8794`；它不是比赛最终部署方案。**不得停止、重启或修改队友共享服务 `10.105.10.4:8793`。**

### 服务与 tunnel（仅在 `:8794` 不健康时）

先检查远端隔离服务；返回 JSON 才可继续。冷启动需要约 3–4 分钟加载模型。

```bash
ssh shihy@10.105.10.4 'curl -fsS http://127.0.0.1:8794/health'
```

若连接失败，以下命令会先检查已有的隔离 Python 进程，避免重复启动；它只操作隔离 `:8794`。

```bash
ssh shihy@10.105.10.4 'if ps -C python -o args= | grep -Fq "/mnt/data2/shihy_archive/p425_seed_isolated_v1/pi05_a54_seed_server.py"; then echo p425_seed_server_already_running; else mkdir -p /mnt/data2/shihy_archive/p425_seed_isolated_v1/tmp /mnt/data2/shihy_archive/p425_seed_isolated_v1/cache; nohup env PYTHONPATH=/home/shihy/lerobot/src CUDA_VISIBLE_DEVICES=2 TMPDIR=/mnt/data2/shihy_archive/p425_seed_isolated_v1/tmp XDG_CACHE_HOME=/mnt/data2/shihy_archive/p425_seed_isolated_v1/cache /home/shihy/miniconda3/envs/lerobot312/bin/python /mnt/data2/shihy_archive/p425_seed_isolated_v1/pi05_a54_seed_server.py --ckpt /mnt/data2/shihy_archive/outputs/pi05_medicine_pour_24d_p425clean_warm_20260824_014320/checkpoints/120000/pretrained_model --root /home/shihy/datasets/medicine_pour_24d_p425 --repo-id medicine_pour_24d_p425 --host 127.0.0.1 --port 8794 --device cuda --seed 292285 >> /mnt/data2/shihy_archive/p425_seed_isolated_v1/seed-server.log 2>&1 < /dev/null & printf "%s\n" "$!" > /mnt/data2/shihy_archive/p425_seed_isolated_v1/seed-server.pid; fi'
```

控制主机没有本地 `127.0.0.1:8794` listener 时，建立专用 tunnel：

```bash
ssh -f -N -o BatchMode=yes -o ExitOnForwardFailure=yes -o ConnectTimeout=8 -L 127.0.0.1:8794:127.0.0.1:8794 shihy@10.105.10.4
```

### 零动作预检

以下命令校验固定 server/model/stats/seed 身份和本地运行环境；**不会连接或命令机器人**。

```bash
cd /home/user/github-product/g1-dex3-care-vla && bash scripts/p425_team_fsm_r2/run_fsm_pour.sh --preflight
```

### 受监督实机运行

仅在 G1 已吊装、现场清空、有人监护、服务健康和零动作预检均通过后执行。该命令是本次对机器人的明确授权：最多 120 秒、最多 3 次 attempt；抓取已确认后 12 秒仍未出现倒药目标会安全结束该 attempt 并走既有重试/收尾，不能把它当作倒药成功。

```bash
cd /home/user/github-product/g1-dex3-care-vla && bash scripts/p425_team_fsm_r2/run_fsm_pour.sh --confirm-p425-team-fsm-supervised-run
```

出现异常响声、跳变、人员进入危险范围、图像/追踪异常或不符合预期的动作时，在该运行终端按 `Ctrl+C`。runner 会进入既有 `finally` 收尾并尝试双臂回 home；Dex3 保持最后目标，随后按现场流程人工接管或断电。每次运行的 artifact 保存到 `scripts/p425_team_fsm_r2/runs/run-<UTC时间>/`，保留该目录和现场视频后再分析最早失败阶段。



语音：
cd /home/unitree/medication_robot

./run_cloud.sh