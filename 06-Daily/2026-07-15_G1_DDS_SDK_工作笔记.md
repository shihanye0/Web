# 2026-07-15｜G1 DDS / SDK 调试工作笔记

## 今日目标

建立 G1 的只读状态接收基线：确认网络、DDS 多播、SDK reader 与候选 `rt/lowstate` 状态源，并为下一次的“状态显示 → 规则事件”闭环准备可重复验证器。

## 今日得到的实机结果

| 项目 | 结果 |
|---|---|
| 设备与网络 | G1 EDU+ CN、23DoF；主机 `192.168.123.99/24` 与 G1 `192.168.123.161` 的 Ping 4/4 成功 |
| 调试状态 | 操作者确认已进入调试模式 |
| RTPS / DDS 流量 | G1 持续发送 `239.255.0.1:7400/7401` 流量；Domain 0 是候选值，尚未冻结 |
| 原始多播接收 | 显式绑定 `192.168.123.99` 的 6 秒只读 probe 收到 6,706 包、13,994,200 字节，主源为 `192.168.123.161:48457` |
| 防火墙 | UFW 已放行 G1 到 `239.255.0.1/udp` 的入站流量，计数证明规则命中 |
| SDK reader | SDK 可启动、加入 `239.255.0.1`，并创建 UDP 7400/7401 socket；`rt/lowstate` + HG `LowState_` 的 10 秒候选订阅仍为 `SAMPLES=0` |

## 今日没有完成的部分

Gate 1 尚未通过：没有获得“真实 SDK callback 收到 `LowState_`”的样本证据。因此未开始状态显示、规则事件、ROS 2、Dex3 或任何控制接口。

当前命令执行器不拥有主机的物理 `enp3s0` 网络命名空间。最新预检结果为：

```text
execution_context=invalid_missing_g1_interface_or_address
required_interface=enp3s0
required_address=192.168.123.99
ddsls_exit=not_run
sdk_probe_exit=not_run
```

这只说明执行器隔离，不能据此判断主机或机器人故障。

## 已准备的下一次验证器

临时诊断目录：`/tmp/g1-dds-probe/`。

- `build/g1_lowstate_address_probe`：地址绑定的 HG `LowState_` SDK reader；只创建 reader。
- `run_full_readonly_alignment.sh`：先预检真实 `enp3s0 / 192.168.123.99`，再进行 DDS discovery 和 60 秒 SDK 采样；reader 存活期间保存多播成员和 UDP socket 快照。

下一次只需在实际连接 G1 网线的 Ubuntu 主机网络命名空间中运行该验证器。通过条件：

```text
ddsls_json_present=yes
sdk_probe_exit=0
sdk-probe.txt 中 SAMPLES > 0
```

通过后才将 Domain、topic、类型和频率写入活动合同，并开始 SDK 状态显示与规则事件。

## 文档与证据

- [Gate 0 网络报告](gate0_network_probe_20260715.md)
- [Gate 1 DDS / SDK 统一诊断](gate1_dds_sdk_alignment_20260715.md)
- [实机就绪门禁](../docs/03-实机就绪门禁.md)
- [SDK / DDS / ROS 2 通信闭环](../docs/08-SDK_DDS_ROS2_通信闭环.md)

原始包和临时日志保留在 Git 外的 `/tmp/g1-dds-*.pcap` 与 `/tmp/g1-dds-probe/`。今天未创建机器人命令 publisher、动作 RPC、Dex3 写入或关节控制。

## 交付路径说明

用户要求的 `/home/user/Web/06-Daily` 不在本会话可写文件系统范围内，故本笔记暂存为项目内回退交付物：

```text
/home/user/github-product/g1-dex3-care-vla/reports/2026-07-15_G1_DDS_SDK_工作笔记.md
```
